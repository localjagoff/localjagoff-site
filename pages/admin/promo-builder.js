import Head from "next/head";
import { useEffect, useMemo, useState } from "react";
import PromoAdminNav from "../../components/PromoAdminNav";

const QUEUE_KEY = "localJagoffPromoQueue";
const BANK_KEY = "localJagoffProductPromoBank";

const PLATFORMS = [["facebook", "Facebook"], ["instagram", "Instagram"], ["tiktok", "TikTok"], ["youtube_shorts", "YouTube Shorts"]];
const HOOKS = ["New drop for anyone who knows exactly what a jagoff is.", "Western PA attitude, cleaned up just enough for public viewing.", "Not tourist gear. Not fake tough. Just Local Jagoff.", "For the locals, the loud ones, and the beautifully difficult ones.", "A little local pride. A little smart mouth. That is the brand.", "Clean enough to wear out. Jagoff enough to feel right."];
const CTAS = ["Grab it at localjagoff.com.", "Shop the drop at localjagoff.com.", "Get yours at localjagoff.com.", "Shop Local Jagoff before someone else gets loud about it.", "Check the drop at localjagoff.com."];
const OVERLAYS = ["NEW DROP LIVE", "LOCAL JAGOFF ENERGY", "FOR THE LOCALS", "BUILT FOR JAGOFFS", "WESTERN PA READY", "LOCALJAGOFF.COM"];

function readArray(key) { if (typeof window === "undefined") return []; try { const parsed = JSON.parse(window.localStorage.getItem(key) || "[]"); return Array.isArray(parsed) ? parsed : []; } catch { return []; } }
function writeArray(key, value) { if (typeof window !== "undefined") window.localStorage.setItem(key, JSON.stringify(Array.isArray(value) ? value : [])); }
function copyText(value) { if (!value || typeof navigator === "undefined") return false; navigator.clipboard.writeText(value); return true; }
function todayIso() { return new Date().toISOString().slice(0, 10); }
function slugify(value) { return String(value || "promo").toLowerCase().trim().replace(/[^a-z0-9]+/g, "-").replace(/^-+|-+$/g, "") || "promo"; }
function platformLabel(value) { return PLATFORMS.find(([key]) => key === value)?.[1] || value; }
function productName(product) { return product?.name || "Local Jagoff Product"; }
function productImage(product) { return product?.thumbnail_url || product?.image || product?.image_url || ""; }
function productUrl(product) { return product?.id ? `https://www.localjagoff.com/product/${product.id}` : "https://www.localjagoff.com"; }
function platformUrl(product, platform) { const params = new URLSearchParams({ utm_source: platform, utm_medium: "social", utm_campaign: "builder-post", utm_content: slugify(productName(product)) }); return `${productUrl(product)}?${params.toString()}`; }
function productType(product) { const name = productName(product).toLowerCase(); if (name.includes("hoodie")) return "hoodie"; if (name.includes("shirt") || name.includes("tee")) return "shirt"; if (name.includes("hat") || name.includes("cap")) return "hat"; if (name.includes("724")) return "724 gear"; return "gear"; }
function hashtags(product, platform) { const type = productType(product); const base = ["#LocalJagoff", "#Pittsburgh", "#WesternPA", "#Yinzer"]; if (productName(product).includes("724")) base.push("#724"); else base.push("#412", "#724"); if (type === "hoodie") base.push("#HoodieSeason"); if (type === "shirt") base.push("#PittsburghShirts"); if (type === "hat") base.push("#PittsburghHats"); if (platform === "tiktok") base.push("#Streetwear", "#SmallBusiness"); return base; }

function makeCaptions(product, platform) {
  const name = productName(product);
  const type = productType(product);
  const local = name.includes("724") ? "724 / Western PA" : "Pittsburgh / Western PA";
  return [`⚡ New from Local Jagoff.\n\n${name}. ${local} ${type} with enough attitude to make it feel right.\n\nGrab yours before another jagoff does.`, `${name} is live.\n\nLocal gear for people who can take a joke, give one back, and still somehow have good taste.`, `A little black-and-gold attitude. A little local mouth.\n\n${name} from Local Jagoff is ready to go.`, `Not tourist gear. Not fake tough.\n\n${name}. Built for the locals who get it.`, `For the jagoff who says they are leaving in five and is absolutely not.\n\n${name} is live now.`, `Fresh drop from Local Jagoff.\n\n${name}. Local gear with a little attitude built in.`];
}

function makeScripts(product) { const name = productName(product); return [`Scene 1: Product pops on a black-and-gold background. Text: NEW DROP LIVE.\nScene 2: Quick zoom on design. Text: ${name}.\nScene 3: End card. Text: LOCALJAGOFF.COM. Voiceover: Shop Local Jagoff before someone else gets loud about it.`, `Scene 1: Start with product closeup. Text: LOCAL JAGOFF ENERGY.\nScene 2: Show full product. Text: FOR THE LOCALS.\nScene 3: Site URL and CTA. Voiceover: Grab it at localjagoff.com.`, `Scene 1: Fast intro cut. Text: NOT TOURIST GEAR.\nScene 2: Product detail. Text: ${name}.\nScene 3: Logo/site. Text: LOCALJAGOFF.COM.`]; }
function defaultSelection() { return { hook: 0, caption: 0, cta: 0, hashtags: 0, overlay: 0, script: 0 }; }
function buildOptions(product, platform) { const tagSets = [hashtags(product, platform), hashtags(product, platform).filter((tag) => tag !== "#Yinzer"), ["#LocalJagoff", "#Pittsburgh", "#WesternPA", "#SmallBusiness"]]; return { hooks: HOOKS, captions: makeCaptions(product, platform), ctas: CTAS, hashtags: tagSets, overlays: OVERLAYS, scripts: makeScripts(product) }; }

function buildFinal({ product, platform, options, selected }) {
  const hook = options.hooks[selected.hook] || "";
  const caption = options.captions[selected.caption] || "";
  const cta = options.ctas[selected.cta] || "";
  const tagLine = (options.hashtags[selected.hashtags] || []).join(" ");
  const overlay = options.overlays[selected.overlay] || "";
  const script = options.scripts[selected.script] || "";
  const link = platformUrl(product, platform);
  if (platform === "youtube_shorts") return [`YouTube Shorts Title:\n${productName(product)} | Local Jagoff Drop`, `Description:\n${hook}\n\n${caption}\n\n${cta}\nLink:\n${link}\n\n${tagLine}`, `Overlay Text:\n${overlay}`, `Short Video Script:\n${script}`].join("\n\n---\n\n");
  return [`${platformLabel(platform)} Post:\n${hook}\n\n${caption}\n\n${cta}`, `First Comment:\n${cta} 🖤💛`, `Link:\n${link}`, `Hashtags:\n${tagLine}`, `Image Overlay Text:\n${overlay}`, `Short Video Script:\n${script}`].join("\n\n");
}

function bankEntry(product, type, platform, text, tag) { return { id: `bank-${Date.now()}-${Math.random().toString(16).slice(2)}`, createdAt: new Date().toISOString(), productId: String(product?.id || ""), productName: productName(product), productImage: productImage(product), productCategory: product?.category || productType(product), type, platform, text, source: "Promo Builder", tag, status: "Approved" }; }
function sectionText(text, label) { const value = String(text || ""); const start = value.indexOf(`${label}:`); if (start === -1) return ""; const after = value.slice(start + label.length + 1); const next = after.search(/\n\n[A-Z][A-Za-z\s/]+:/); return (next >= 0 ? after.slice(0, next) : after).trim(); }
function previewParts(finalPost, platform, product) { return { caption: sectionText(finalPost, `${platformLabel(platform)} Post`) || finalPost, firstComment: sectionText(finalPost, "First Comment"), link: sectionText(finalPost, "Link") || platformUrl(product, platform), hashtags: sectionText(finalPost, "Hashtags"), overlay: sectionText(finalPost, "Image Overlay Text") || sectionText(finalPost, "Overlay Text"), script: sectionText(finalPost, "Short Video Script"), description: sectionText(finalPost, "Description"), title: sectionText(finalPost, "YouTube Shorts Title") }; }

function PlatformPreview({ product, platform, finalPost }) {
  const parts = previewParts(finalPost, platform, product);
  const image = productImage(product);
  const isShortVideo = platform === "tiktok" || platform === "youtube_shorts";
  const caption = platform === "youtube_shorts" ? parts.description : parts.caption;
  return <section className={`preview preview${platform}`}><div className="previewTop"><div className="avatar">LJ</div><div><strong>Local Jagoff</strong><span>{platformLabel(platform)} preview • rough layout only</span></div></div><div className="previewBody"><p>{caption || "Your final caption will preview here."}</p>{parts.hashtags && <p className="tags">{parts.hashtags}</p>}</div><div className={isShortVideo ? "media videoMedia" : "media"}>{image ? <img src={image} alt={productName(product)} /> : <div className="noImage">Product Image</div>}{parts.overlay && <div className="overlayText">{parts.overlay}</div>}</div><div className="linkCard"><span>LOCALJAGOFF.COM</span><strong>{platform === "youtube_shorts" && parts.title ? parts.title : productName(product)}</strong><small>{parts.link}</small></div>{parts.firstComment && <div className="comment"><strong>First comment</strong><p>{parts.firstComment}</p></div>}{parts.script && <details className="script"><summary>Short video script</summary><pre>{parts.script}</pre></details>}</section>;
}

export default function PromoBuilder() {
  const [products, setProducts] = useState([]);
  const [productId, setProductId] = useState("");
  const [platform, setPlatform] = useState("facebook");
  const [scheduledDate, setScheduledDate] = useState(todayIso());
  const [options, setOptions] = useState(null);
  const [selected, setSelected] = useState(defaultSelection());
  const [finalEdit, setFinalEdit] = useState("");
  const [message, setMessage] = useState("");

  useEffect(() => { fetch("/api/get-products").then((res) => res.json()).then((data) => { const list = Array.isArray(data) ? data : []; setProducts(list); setProductId(list[0]?.id ? String(list[0].id) : ""); }).catch(() => setProducts([])); }, []);
  const product = useMemo(() => products.find((item) => String(item.id) === String(productId)) || products[0] || null, [products, productId]);
  const generatedFinal = useMemo(() => options && product ? buildFinal({ product, platform, options, selected }) : "", [product, platform, options, selected]);
  const finalPost = finalEdit || generatedFinal;
  useEffect(() => { setFinalEdit(generatedFinal); }, [generatedFinal]);

  const generateOptions = () => { if (!product) { setMessage("Pick a product first."); return; } const nextOptions = buildOptions(product, platform); setOptions(nextOptions); setSelected(defaultSelection()); setMessage("Options generated. Pick the parts you want, then edit and preview the final."); };
  const resetFinal = () => { setFinalEdit(generatedFinal); setMessage("Final post reset to the generated version."); };
  const saveToQueueWithStatus = (status) => { if (!finalPost || !product) { setMessage("Build a final post first."); return; } const queue = readArray(QUEUE_KEY); const item = { id: `builder-${Date.now()}`, queueId: `queue-${Date.now()}-${Math.random().toString(16).slice(2)}`, createdAt: new Date().toISOString(), queuedAt: new Date().toISOString(), source: "Promo Builder", mode: "builder", platform, displayPlatform: platform, scheduledPlatform: platform, scheduledDate, status, product, promo: { brand_angle: "Built from selected Promo Builder parts.", builder_final: finalPost, facebook_post: platform === "facebook" ? finalPost : "", instagram_caption: platform === "instagram" ? finalPost : "", tiktok_caption: platform === "tiktok" ? finalPost : "", youtube_shorts_description: platform === "youtube_shorts" ? finalPost : "", cta: finalPost } }; writeArray(QUEUE_KEY, [item, ...queue].slice(0, 500)); setMessage(`Edited final post saved to Queue as ${status}.`); };
  const saveSelectedToBank = () => { if (!options || !product) { setMessage("Generate options first."); return; } const bank = readArray(BANK_KEY); const entries = [bankEntry(product, "hook", platform, options.hooks[selected.hook], "Builder selected hook"), bankEntry(product, "caption", platform, options.captions[selected.caption], "Builder selected caption"), bankEntry(product, "cta", platform, options.ctas[selected.cta], "Builder selected CTA"), bankEntry(product, "overlay", "general", options.overlays[selected.overlay], "Builder selected overlay"), bankEntry(product, "note", platform, options.scripts[selected.script], "Builder selected script")].filter((entry) => entry.text); writeArray(BANK_KEY, [...entries, ...bank].slice(0, 800)); setMessage(`${entries.length} selected part${entries.length === 1 ? "" : "s"} saved to Product Bank.`); };
  const choiceBlock = (title, keyName, items, render = (item) => item) => items?.length ? <section className="choiceBlock"><h2>{title}</h2>{items.map((item, index) => <label key={`${keyName}-${index}`} className={`choice ${selected[keyName] === index ? "chosen" : ""}`}><input type="radio" name={keyName} checked={selected[keyName] === index} onChange={() => setSelected({ ...selected, [keyName]: index })} /><span>{render(item)}</span></label>)}</section> : null;

  return <div className="page"><Head><title>Local Jagoff Promo Builder</title><meta name="robots" content="noindex,nofollow" /></Head><PromoAdminNav /><main className="wrap"><header className="hero"><p className="kicker">PRIVATE ADMIN TOOL</p><h1>Promo Builder</h1><p>Generate options, pick the parts, edit the final wording, preview the post, then save it as approved or pending.</p></header><section className="controls"><label>Product<select value={productId} onChange={(e)=>setProductId(e.target.value)}>{products.map((item)=><option key={item.id} value={item.id}>{item.name}</option>)}</select></label><label>Platform<select value={platform} onChange={(e)=>setPlatform(e.target.value)}>{PLATFORMS.map(([value,label])=><option key={value} value={value}>{label}</option>)}</select></label><label>Schedule Date<input type="date" value={scheduledDate} onChange={(e)=>setScheduledDate(e.target.value)} /></label><button type="button" onClick={generateOptions}>Generate Options</button></section>{message && <section className="message">{message}</section>}{options && <section className="builderGrid"><div>{choiceBlock("Choose Hook", "hook", options.hooks)}{choiceBlock("Choose Caption", "caption", options.captions)}{choiceBlock("Choose CTA", "cta", options.ctas)}{choiceBlock("Choose Hashtags", "hashtags", options.hashtags, (item)=>item.join(" "))}{choiceBlock("Choose Overlay", "overlay", options.overlays)}{choiceBlock("Choose Script", "script", options.scripts)}</div><aside className="finalPanel"><p className="kicker">FINAL BUILD</p><h2>{platformLabel(platform)} Final Post</h2><label className="finalLabel">Edit final before saving<textarea value={finalEdit} onChange={(e)=>setFinalEdit(e.target.value)} /></label>{product && <PlatformPreview product={product} platform={platform} finalPost={finalPost} />}<div className="actions"><button type="button" onClick={()=>setMessage(copyText(finalPost) ? "Copied edited final post." : "Nothing to copy.")}>Copy Final</button><button type="button" onClick={()=>saveToQueueWithStatus("Approved")}>Save as Approved</button><button type="button" onClick={()=>saveToQueueWithStatus("Needs Review")} className="secondary">Save as Needs Review</button><button type="button" onClick={saveSelectedToBank} className="secondary">Save Parts to Bank</button><button type="button" className="secondary" onClick={resetFinal}>Reset Final</button></div></aside></section>}</main><style jsx>{`.page{min-height:100vh;padding:0 16px 80px;color:#fff;background:radial-gradient(circle at top left,rgba(255,230,0,.15),transparent 30%),linear-gradient(180deg,#050505,#000)}.wrap{max-width:1320px;margin:0 auto;padding-top:34px}.hero,.controls,.message,.choiceBlock,.finalPanel{background:rgba(13,13,13,.9);border:1px solid rgba(255,230,0,.18);border-radius:24px;box-shadow:0 20px 70px rgba(0,0,0,.35)}.hero{padding:26px;margin-bottom:14px}.kicker{margin:0 0 10px;color:#ffe600;font-size:12px;font-weight:900;letter-spacing:2px;text-transform:uppercase}.hero h1{font-size:clamp(44px,8vw,96px);line-height:.9;text-transform:uppercase}.hero p{color:#ddd;line-height:1.55}.controls{display:grid;grid-template-columns:2fr 1fr 1fr auto;gap:12px;align-items:end;padding:16px;margin-bottom:14px}label{display:block;color:#ffe600;font-size:12px;font-weight:900;letter-spacing:1px;text-transform:uppercase}select,input,textarea{width:100%;margin-top:8px;color:#fff;background:#050505;border:1px solid #333;border-radius:14px;padding:12px}textarea{min-height:300px;resize:vertical;line-height:1.55}button{border:none;border-radius:14px;padding:12px 14px;cursor:pointer;font-weight:900;background:#ffe600;color:#000}.secondary{background:#1b1b1b;color:#fff;border:1px solid #333}.message{padding:14px;margin-bottom:14px;color:#ffe600;font-weight:900}.builderGrid{display:grid;grid-template-columns:minmax(0,1fr) 500px;gap:14px;align-items:start}.choiceBlock{padding:16px;margin-bottom:14px}.choiceBlock h2,.finalPanel h2{text-transform:uppercase;color:#ffe600}.choice{display:grid;grid-template-columns:auto minmax(0,1fr);gap:10px;padding:12px;margin:8px 0;border:1px solid #2a2a2a;border-radius:14px;background:#050505;color:#f2f2f2;line-height:1.45;text-transform:none;letter-spacing:0;font-size:14px}.choice.chosen{border-color:#ffe600;background:rgba(255,230,0,.08)}.choice input{width:auto;margin:3px 0 0}.finalPanel{position:sticky;top:76px;padding:16px}.actions{display:grid;gap:10px;margin-top:12px}.preview{margin:14px 0;background:#090909;border:1px solid rgba(255,230,0,.2);border-radius:20px;overflow:hidden}.previewTop{display:flex;gap:10px;align-items:center;padding:12px;border-bottom:1px solid #222}.avatar{display:grid;place-items:center;width:40px;height:40px;border-radius:999px;background:#ffe600;color:#000;font-weight:900}.previewTop strong,.previewTop span{display:block}.previewTop span{margin-top:3px;color:#aaa;font-size:12px}.previewBody{padding:12px}.previewBody p{white-space:pre-wrap;color:#f2f2f2;line-height:1.45;margin:0 0 10px}.tags{color:#ffe600!important;font-weight:800}.media{position:relative;display:grid;place-items:center;min-height:250px;background:linear-gradient(135deg,#171717,#050505)}.videoMedia{min-height:400px}.media img{max-width:100%;max-height:410px;object-fit:contain}.noImage{display:grid;place-items:center;width:100%;height:250px;color:#888;text-transform:uppercase;font-weight:900}.overlayText{position:absolute;left:18px;right:18px;bottom:18px;padding:10px 12px;border-radius:14px;background:rgba(0,0,0,.72);color:#ffe600;text-align:center;font-size:clamp(18px,4vw,34px);font-weight:1000;text-transform:uppercase;letter-spacing:1px}.linkCard{padding:12px;background:#f2f2f2;color:#111}.linkCard span,.linkCard strong,.linkCard small{display:block}.linkCard span{font-size:11px;color:#555;font-weight:900;letter-spacing:1px}.linkCard strong{margin:4px 0;text-transform:uppercase}.linkCard small{color:#666;overflow-wrap:anywhere}.comment{padding:12px;border-top:1px solid #222;background:#050505}.comment strong{color:#ffe600;text-transform:uppercase;font-size:12px;letter-spacing:1px}.comment p{white-space:pre-wrap;color:#eee;line-height:1.45}.script{padding:12px;border-top:1px solid #222;background:#050505}.script summary{cursor:pointer;color:#ffe600;font-weight:900;text-transform:uppercase}.script pre{white-space:pre-wrap;color:#eee;line-height:1.45}@media(max-width:1100px){.controls,.builderGrid{grid-template-columns:1fr}.finalPanel{position:static}textarea{min-height:300px}}`}</style></div>;
}
